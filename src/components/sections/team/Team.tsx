import { H2, P, SectionWrapper } from "@/global-styles/global";

export default function Team() {
  return (
    <SectionWrapper>
      <H2> Oto nasz zespół:</H2>
      <P>Do dokończenia (potrzeba zdjęć i szczegółów)</P>
    </SectionWrapper>
  );
}
